import React from "react";
//import AirAPI from "./"

// results from API deisplaying 5 cards for ozone(o3), sulfur dioxide(so2), nitrogen dioxide(no2), PM25( particles),PM10 (particles). all clickable taking user to AQI table
export default function Air() {
    return (
        <div>

        </div>
    )
    
}